# Private seed / working notes (not part of the public repo)
notes/

# Runtime capture data
traces/
data/
data.sqlite
data.sqlite-*
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Benchmark artifacts (agent-produced files snapshotted by run.sh --save-artifacts)
benchmarks/artifacts/

# Benchmark run outputs (results, verify logs, reports — always produced by run.sh)
benchmarks/runs/

# Local config with secrets/overrides
config.toml
config.local.*
.env
.env.*

# Node
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
npm-debug.log*
.pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
