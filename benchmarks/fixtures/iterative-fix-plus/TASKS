# iterative-fix-plus — single combined task: fix bugs, implement methods.
# The agent works through everything in one session so the entire run
# can be compared across agents as a single data point.
iterative-fix-plus|You have two objectives. Complete them in order, running tests after each change to verify progress. Run `node --test test/*.test.js` to see which tests fail. Do not modify any test files. OBJECTIVE 1 Fix all bugs: There are multiple bugs across src/ causing test failures. After each fix, re-run the tests to confirm progress. Keep going until all tests pass. OBJECTIVE 2 Implement stubbed methods: Three methods in src/ throw 'not implemented'. Implement them to satisfy their JSDoc contracts. 1) PriorityQueue.merge(other) in src/priority-queue.js: copy every (item, priority) pair from other into this queue preserving the min-heap invariant, leave other unchanged, return this. 2) RateLimiter.peekWait(n=1) in src/rate-limiter.js: return milliseconds until n tokens would be available WITHOUT consuming any (0 if already available), accounting for refill over time. 3) ResultCache.topKeys(n) in src/result-cache.js: return keys of the n most-accessed (by successful get() count) non-expired entries, most-accessed first, at most n keys.|TO=$(command -v timeout || command -v gtimeout || true); echo '=== FIXTURE TESTS ==='; ${TO:+$TO -s KILL 120} node --test test/*.test.js && fix_rc=0 || fix_rc=$?; cp "$AAP_BENCH_REF"/*.test.js test/; echo '=== EDGE TESTS ==='; ${TO:+$TO -s KILL 120} node --test test/edge-cases.test.js test/methods.test.js test/throttle-edge.test.js && edge_rc=0 || edge_rc=$?; [ $fix_rc -eq 0 ] && [ $edge_rc -eq 0 ]
